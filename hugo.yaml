baseURL: ""
languageCode: en-us
title: IMPulse – Incident Management Platform
theme: impulse
publishDir: "publish"
resourceDir: "resources"

params:
  description: "Smart routing → right person, right time, no spam. Lightweight incident management platform."
  github_url: "https://github.com/eslupmi/impulse"

# Use content/_index.md as homepage instead of index.md
disableKinds: ["taxonomy", "term"]

markup:
  goldmark:
    renderer:
      unsafe: true
